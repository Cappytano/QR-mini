{
  "name": "qr-logger",
  "version": "6.0.2",
  "private": true,
  "description": "QR Logger (PWA) with Remote Camera (WebRTC), cooldown & enhanced decoder, delayed weight capture + photo",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}